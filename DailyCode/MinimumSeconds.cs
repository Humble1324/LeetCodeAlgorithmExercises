using System.Collections.Generic;

namespace LeetCode.DailyCode;

public partial class DailyCode
{
    public int MinimumSeconds(IList<int> nums)
    {
        //把所有格子变成每种数字的时长写进字典，找最小的导出
        //todo:做不来以后再说
        return 0;
    }
}